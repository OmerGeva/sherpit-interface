import styled from 'styled-components'

export const MiddlemanHomepageContainer = styled.div`
    padding: 5vw;
    position: relative;
    .orders-container{
        display: flex;
        flex-wrap: wrap;
    }
    
`